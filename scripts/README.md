# Scripts

Utilities for dataset download and preparation, synthetic demo-data generation, and database seeding. Scripts should be repeatable, accept explicit input/output paths, and never download data implicitly during application startup.