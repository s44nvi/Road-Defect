# Notebooks

Use notebooks for exploratory sensor analysis, model evaluation, and reproducible experiments. Production scoring and inference logic belongs in `ml/` or `backend/`, with notebooks importing those modules rather than becoming the source of truth.