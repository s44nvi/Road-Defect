# Backend

FastAPI application boundary for authentication, request validation, domain commands, read models, and lifecycle transitions.

Keep long-running inference, clustering, and repair validation in asynchronous worker jobs. Suggested modules are `app/api`, `app/models`, `app/services`, and `app/db`.