# Database

PostgreSQL with PostGIS is the source of truth for observations, persistent defects, road segments, verification decisions, crews, repairs, and audit events.

The eventual `schema.sql` and `seed.sql` files should be safe to run against a fresh local database and should mark all generated demo records as synthetic.